/*ELEMENTOS NOVOS
//previousElementSibling, o nextElementSibling = obter elemento irmao anterior ou posterior

//getBoundingClientRect = obter retângulo de cliente delimitador

//window.addEventListener('resize', function) = quando pagina for redimensionar

*/
const p = window.document.querySelector('p')
